/**
 * 配置文件
 * @type {{controller: {URI: null}, staticDir: string, viewDir: string}}
 */
var config = {
	controller: {
		URI: null
	},
	staticDir: 'static',
	viewDir: 'View'
}

module.exports = config;